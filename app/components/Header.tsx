"use client";
import React, { ReactElement } from "react";

function Header(): ReactElement {
  return (
    <header className="w-full bg-bg-main text-text-main text-center p-5 m-5 text-lg font-bold">
      momen.ai
    </header>
  );
}

export default Header;
